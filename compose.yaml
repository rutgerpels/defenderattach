services:
  dashboard:
    build: .
    ports:
      - "8050:8050"
    volumes:
      - ./inputfolder:/app/inputfolder:ro
      - ./inputfolder_opty:/app/inputfolder_opty:ro
      - ./output:/app/output
    environment:
      APP_HOST: 0.0.0.0
      APP_PORT: 8050
      APP_DEBUG: "false"
