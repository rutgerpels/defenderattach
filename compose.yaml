services:
  dashboard:
    build: .
    ports:
      - "8050:8050"
    volumes:
      - ./inputfolder:/app/inputfolder:ro
      - ./inputfolder_opty:/app/inputfolder_opty:ro
      - ./output:/app/output
    environment:
      DASH_HOST: 0.0.0.0
      DASH_PORT: 8050
      DASH_DEBUG: "false"
